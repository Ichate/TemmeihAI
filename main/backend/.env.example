# run mode: local (default) or production
# local      = your own machine, no limits, saved api keys, multiple bots
# production = public hosting, per-browser users, limits on, no saved keys
APP_MODE=local
